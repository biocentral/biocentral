[project]
name = "biocentral-monorepo"
version = "2.0.0"
description = "Monorepo for Biocentral projects"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pre-commit>=4.0.0",
]
